#include <bits/stdc++.h>
#define ioopti ios_base::sync_with_stdio(false), cin.tie(nullptr);

using namespace std;
int main(){
	ioopti
	int a, b;
	cin >> a >> b;
	for(int i = 0; i < a+b; i++){
		int ass;
		cin >> ass;
	}
	cout << a - b;
}
