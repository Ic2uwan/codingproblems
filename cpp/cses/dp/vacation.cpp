#include <bits/stdc++.h>
#define amogus ios_base::sync_with_stdio(0);cin.tie(0);
using namespace std;
typedef long long ll;

vector<vector<int>> dp;

int main() {
    amogus
    int n, a, b, c;
    cin >> n;
    dp.resize(n);
}
